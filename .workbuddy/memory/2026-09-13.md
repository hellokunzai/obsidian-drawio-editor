# 2026-09-13

- 修复格式面板底部按钮（编辑/复制样式/粘贴样式/设置为默认样式）在「文本」「排列」Tab 下仍显示的问题，改为仅在「样式」Tab 显示。
- 版本号 0.9.0 → 0.9.1（manifest.json / package.json / versions.json）。
- 已通过 tsc 类型检查与 esbuild 生产构建。
- 为格式面板「填充」section 新增渐变选项：渐变复选框、方向下拉框（向下/向上/向左/向右）、渐变颜色选择器；未启用渐变时禁用方向与颜色控件。
- 版本号 0.9.1 → 0.10.0。
- 已通过 tsc 类型检查与 esbuild 生产构建。
- 将「线条」section 的线条类型选择器由文本下拉框改为可视化下拉：触发按钮与菜单项均用 SVG 线条实时预览样式；支持实线、虚线、点线、点划线、双点划线五种；采用 fixed 定位菜单避免被滚动容器裁剪。
- 版本号 0.10.0 → 0.10.1。
- 已通过 tsc 类型检查与 esbuild 生产构建。
- 为格式面板「效果」section 新增四个复选框：圆角、草图、玻璃、阴影（2x2 网格布局）。圆角/玻璃/阴影分别映射 mxConstants 的 STYLE_ROUNDED / STYLE_GLASS / STYLE_SHADOW，草图映射自定义 sketch 样式键；并将阴影从「文本」Tab 移入「效果」section。
- 同步添加中/英文翻译键：format.rounded / format.sketch / format.glass；styles.css 新增 .drawio-fmt-effects-grid 网格样式。
- 版本号 0.10.1 → 0.10.2。
- 已通过 tsc 类型检查与 esbuild 生产构建。
- 删除格式面板底部「粘贴样式」按钮，并清理对应的 `pasteStyle()` 方法与 `copiedStyle` 字段；同步移除中/英文翻译键 `format.pasteStyle`。
- 版本号 0.10.2 → 0.10.3。
- 已通过 tsc 类型检查与 esbuild 生产构建。
- 新增画布图形右键上下文菜单（v0.11.0，draw.io 风格）：新增 `src/TextEditModal.ts`（可复用文本编辑弹窗，供编辑样式/数据/链接/连接点共用）；`DrawioView` 新增 `setupContextMenu`（自绘 DOM 菜单挂 body，`setEnabled(false)` 关掉 mxGraph 自带 popup）、12 项动作、`showCtxMenu/closeCtxMenu/removeCtxOutsideListeners`、`setupCanvasShortcuts`（Ctrl+C/X/V/D + Del/Backspace，悬停画布时接管）。
- 关键修正（坐标语义）：`getCellAt` 要的是**容器像素**坐标，不是图坐标 → 统一改用 `mxUtils.convertPoint(container, clientX, clientY)`。原先写的 `clientX/scale - view.translate.x` 反投影是错的（缩放/平移后会失准）。详见 MEMORY.md。
- 右键从 panning 触发键中移除（`isPanningTrigger` 不再含 `usePopupTrigger && isPopupTrigger`），右键让给上下文菜单；平移仍由中键 / Ctrl+左键(空白) / 空格 / Ctrl+Shift 承担。
- 补齐剪贴板消费方：新增 `pasteCells()`（`graph.moveCells(cells, off, off, true, parent)` 逐次偏移粘贴）、空白处右键出「粘贴」；`duplicateCells()` 改用 `moveCells(...,true)`（正确处理连线终点引用/组合子节点/边控制点）；`copyCells` 重置 `clipboardPasteCount`。
- 便签本：`settings.scratchpad` 持久化 + 面板新增「便签本」分组（仅非空时显示，`filterPalette` 跳过该分组），每项悬停显示「×」移除按钮（`removeFromScratchpad`）；`loadSettings` 对 scratchpad 做数组/结构合法性过滤。
- 移除了原型里标注但未实现且会冲突的快捷键文案（Ctrl+E 会撞 Obsidian「编辑/阅读模式切换」、Ctrl+M 未实现）；实际只保留已实现的 Del / Ctrl+C/X/V/D。
- 已知事实：本版 mxClient（4.2.2）的 `mxKeyHandler` 构造函数**默认不绑定任何按键**（`normalKeys` 为空），删除键必须在插件里自己接。
- 版本号 0.10.3 → 0.11.0（manifest.json / package.json / versions.json 三处同步）。
- 已通过 tsc 类型检查与 esbuild 生产构建，产物校验：24 条中文文案 + 6 个代码标记 + 4 个 CSS 类全部命中，MISS_TOTAL=0。
- 布局修正（v0.11.1，用户反馈「形状栏向下移动一下，不要覆盖顶部的菜单栏」）：把 `buildToolbar` 的父节点从 `.drawio-canvas-area` 改成视图根容器 `container`，工具栏（撤销/重做/缩放/删除/导出/保存）改为横跨整个视图宽度、独占顶部一行；形状面板 `.drawio-palette` 顺位下移到工具栏之下，不再占据顶部那一行。
- 配套 CSS：`.drawio-wrapper` 由 `height: 100%` 改为 `flex: 1 1 auto; min-height: 0`（工具栏变成兄弟节点后 100% 会顶出容器）。
- 版本号 0.11.0 → 0.11.1（三处同步），已通过 tsc 与生产构建，并在 main.js 中确认 `buildToolbar(container)` 先于 `createDiv("drawio-wrapper")` 执行。
- 诊断方法备忘：本机没有 PIL/ImageMagick，但可用 `python -m venv` + `pip install pillow` 后逐像素扫描截图定位元素边界（本次即靠它确认「面板顶部 y=113 与工具栏顶部齐平、视图标头在 y=50~112」，从而判断出用户指的是工具栏那一行）。
- 删除格式面板头部（v0.11.2，用户要求删掉「格式」标题与右侧两个图标按钮）：`FormatPanel.build()` 移除 `.drawio-fmt-header`（标题 span + `.drawio-fmt-actions` 里的折叠/关闭按钮）及其 click 监听；Tab 行 `.drawio-fmt-tabs` 成为面板第一行。
- 同步清理：i18n 中英文表删除 `format.title` / `format.collapse` / `format.close` 三键；styles.css 删除 `.drawio-fmt-header` / `.drawio-fmt-actions` / `.drawio-fmt-icon-btn`（含 hover）规则。
- 关闭面板的能力未丢失：`selectionModel` CHANGE 监听里空选中即 `hide()`（点画布空白处取消选中即收起），「排列」Tab 的删除按钮也会 `hide()`。失去的是「保持选中但收起面板」这条路径。
- 版本号 0.11.1 → 0.11.2（三处同步）；tsc 与生产构建通过；产物校验：4 个旧类名 + 3 个旧 i18n key 在 main.js 中全部 MISSING，`drawio-fmt-tabs/tabpane` 保留，样式旁证 8 条中文文案仍在。
- 新需求（待开发，先出原型）：底部新增「页面栏」（多页 sheet），参考用户给的第二张图（⋮ + 第1页/第2页/第3页 ^ + ＋）。
- 已产出 `prototypes/page-bar-prototype.html`（可交互独立原型，未动 .ts）：含 ⋮ 菜单、页签切换、+ 新建、双击改名、活动页签 ^ 菜单、右键页签菜单、拖拽重排、仅剩一页时删除置灰、页签横向滚动；并有「页面栏范围：画布区 / 整宽」切换用于确认对齐方式（用户截图红框是画布区宽，draw.io 实际也是容器内）。
- 原型取色直接取自用户截图逐像素采样：视图区 #EFF1F5、面板/工具栏 #E6E9EF、页签栏 #DCE0E8、边框 #CCD0DA、强调色 #167dff；选择框绿色 #34c759。
- 关键设计前提（开发时要用）：多页必须共用同一个 `.drawio` 文件，即 `<mxfile>` 下多个 `<diagram name=... id=...>`；当前 `loadDiagram` 只读 `getElementsByTagName("diagram")[0]`、`saveDiagram` 只写一个 `<diagram>`，需要改成「页列表 + 活动页」模型。
- 参考 draw.io 官方行为（已查证）：+ 新建页；右键页签或三点菜单可 Insert / Duplicate / Rename / Delete / Move Left / Move Right；页签可拖动重排；增删页可撤销重做。
- 开发完成（v0.12.0，用户确认原型后动手）：新增 `src/PageBar.ts`（页面栏组件，23 个方法）——页签渲染/切换、+ 新建、双击行内重命名（Enter 确认 / Esc 取消 / blur 保存 / 宽度随文字自适应）、⋮ 与活动页签 ^ 与右键页签与右侧空白右键都弹同一个菜单、拖拽重排（蓝线指示）、仅一页时「删除页面」置灰、页签横向滚动且自动滚到活动页。菜单挂 `document.body`、`position: fixed`，与画布右键菜单同一做法（避免被 `.drawio-pagebar` 裁掉）。
- `DrawioView` 多页改造：新增 `DiagramPage{id,name,xml}` 列表 + `activePage` + `suppressDirty` + `loadError`；`buildUI()` 在 `.drawio-canvas-area` 内、`.drawio-graph-container` 之后插入页面栏（故页面栏宽度 = 画布区宽，右抵格式面板，与 draw.io 一致）。
- 存取改造：`loadDiagram` 遍历 `getElementsByTagName("diagram")` 全部页；`extractModelXml` 兼容三种写法（① 未压缩且 mxGraphModel 是子元素 ② 未压缩但被转义成文本 ③ draw.io 压缩格式 base64+raw deflate）；`saveDiagram` 用 `pages.map(...)` 把每页写成一个 `<diagram name id>`（未压缩 mxGraphModel 文本，draw.io 双端可互通）。
- 切页实现：`captureActivePage()`（把当前画布编码回 `pages[active].xml`）→ `loadPageIntoGraph()`：`model.clear()` 重建干净模型（避免上一页 cell id 残留冲突）→ `codec.decode` → `graph.clearSelection()` → `undoManager.clear()`（撤销栈跨页会串味，每页各自独立）。全程 `suppressDirty=true`，避免切页被当成内容改动而触发写盘。
- ⚠️ 关键 API 事实（实测 mxClient 4.2.2）：`mxGraphModel.prototype.clear = function(){ this.setRoot(this.createRoot()); }` —— 切页前用它重置模型是正确做法。另：mxGraph 自带一套 `mxGraphModel` 专用 codec，其 `decodeRoot` 结尾会 `model.setRoot(rootCell)`，所以 decode 会替换 root，不会把新内容挂到旧 root 上。
- 排序 off-by-one 修正：原型里 `if (slot > from) slot--` 是错的（`slot` 的坐标系本来就是「抽掉被拖页签后的数组」，正好等价于 splice 后要插入的下标），会导致「把页签拖到最右却没动」。正式实现已去掉该补偿，改为 `const to = slot`（`src/PageBar.ts` 的 `onDragUp`）。
- 数据安全护栏：新增 `loadError`，文件解析失败时画布给一个空白页但**禁止自动保存**（`markDirty` 提前 return、`cleanup` 也加 `!loadError` 守卫），避免把用户原文件覆盖成空 mxfile；手动保存不受限。
- 删除一页后活动页修正：删前面的页只 `activePage-1`（画布内容不变，不重载）；删当前页则改为 `min(index, len-1)` 并重载；删后面的页不动。`movePage` 的 activePage 修正已按三种情形分别处理。
- 版本号 0.11.2 → 0.12.0（三处同步）；tsc 退出码 0；esbuild --production 成功；产物校验 MISS_TOTAL=0（11 个新类名 + 12 个 i18n key + 12 条中文文案 + 9 个实现标记全命中，旧文案 `图表已保存`/`加载图表失败：` 仍在）；另用 Python xml.etree 验证了两页 mxfile 的 XML 往返（diagram 数=2、name/id 保留、每页 root 首个 cell id=0）与压缩分支判定。
- 安全自检：`src/PageBar.ts` 无 `createContextualFragment` / `new Function` / `eval(`；DOM 一律用 `createDiv`/`createEl`/`createSpan`，`innerHTML` 仅用于静态 SVG 图标字符串（与既有 buildToolbar 一致）。方法重名检查：DrawioView 82 / PageBar 23 / FormatPanel 19，无重名。
